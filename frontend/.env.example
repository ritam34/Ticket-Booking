# In dev, requests to /api are proxied to this backend URL (see vite.config.js)
VITE_API_PROXY_TARGET=http://localhost:5000

# In production (e.g. Vercel), set the full backend URL here instead
VITE_API_URL=https://your-backend.onrender.com/api
